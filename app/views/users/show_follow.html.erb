<!DOCTYPE html>
<html lang="en">
<head>
</head>

<body>

  <br/><br/><br/>
  <div class="container">

    <% provide(:title, @title) %>
    <div class="row">
      <br/>
      <h1><%= @user.username %></h2>
      <h3><%= link_to "view my profile", "/users/#{@user.username}" %></h3>
      <%= render 'shared/stats' %>

      <div class="col-md-6">
        <h3>Following:</h3>
        <% if @followings.any? %>
          <% @followings.each do |user| %>
            <div style="text-align: center;">
            <%= link_to user.username, "/users/#{user.username}" %>
            </div>
          <% end %>
        <% end %>
      </div>

      <div class="col-md-6">
        <h3>Followers:</h3>
        <% if @followers.any? %>
          <% @followers.each do |user| %>
            <div style="text-align: center;">
            <%= link_to user.username, "/users/#{user.username}" %>
            </div>
          <% end %>
        <% end %>
      </div>

    </div> <!-- row -->
  </div> <!-- container -->

<!-- Bootstrap core JavaScript
================================================== -->
<!-- Placed at the end of the document so the pages load faster -->

<%= javascript_include_tag "https://ajax.googleapis.com/ajax/libs/jquery/1.11.3/jquery.min.js" %>
<%= javascript_include_tag "bootstrap.min" %>
</body>
</html>
