<% unless signed_in?(@user) %>
  <% if @user.username != current_user.username %>
    <div id="follow_form">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow' %>
    <% else %>
      <%= render 'follow' %>
    <% end %>
    </div>
  <% end %>
<% end %>
